name: Test DCX Custom Action

on:
  push:
    branches:
      - main

jobs:
  run-dcx-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run custom DCX action
        uses: your-username/dcx-public-action@main
